<h1> <%= @job.title %> </h1>
<div class="text-center">
  <%= link_to("Upload Resume", new_job_resume_path(@job), :class => "btn btn-primary btn-warning") %>
</div>
<p>
  <%= simple_format(@job.description) %>

</p>
